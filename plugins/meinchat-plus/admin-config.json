{
  "fields": [
    {
      "key": "debug_mode",
      "type": "boolean",
      "label": "Debug mode",
      "default": false,
      "description": "Verbose client-side crypto logging (never logs key material or plaintext)."
    },
    {
      "key": "one_time_prekey_count",
      "type": "number",
      "label": "One-time prekeys per device",
      "default": 100,
      "description": "How many one-time prekeys to generate + upload at registration."
    },
    {
      "key": "one_time_prekey_low_water",
      "type": "number",
      "label": "One-time prekey low-water mark",
      "default": 20,
      "description": "Refill one-time prekeys when the server signals the pool dropped below this."
    }
  ]
}
