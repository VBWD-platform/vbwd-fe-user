# S47.2 cache-bypass marker (parity with nginx.prod.conf.template). In DEV the
# `location /` block proxies EVERYTHING to the Vite dev server, so every visitor
# already gets the live CSR SPA — there is no static `${VAR_DIR}/seo` serving in
# dev (no built dist, no prerender mount). The prerender → SPA hand-off is a
# PROD-only serving path; the dev↔prod template diff is documented in
# plugins/cms/docs/seo/nginx-prerender.md. The map is declared here only to keep
# both templates aligned for review.
map $http_cookie $vbwd_is_authed {
    default 0;
    "~*vbwd_session=" 1;
}

server {
    listen 80;
    resolver 127.0.0.11 valid=5s;

    # Allow large image uploads (raw photo / base64-inflated e2e ciphertext).
    # Must exceed meinchat `attachment_max_bytes` (25 MB) + ~33% base64 overhead.
    client_max_body_size 40m;

    set $vite http://dev:5173;
    set $backend http://host.docker.internal:5000;
    set $pluginapi http://plugin-api:3001;

    # Proxy Plugin API requests
    location /_plugins {
        proxy_pass $pluginapi;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Proxy uploaded media files to backend
    location /uploads/ {
        proxy_pass $backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # SSE event stream — never buffer (browser would only see messages on
    # refresh). Exact-match ignores the ?stream_token=… query.
    location = /api/v1/messaging/stream {
        proxy_pass $backend;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 3600s;
        add_header X-Accel-Buffering no always;
    }

    # Proxy API requests to backend
    location /api/ {
        proxy_pass $backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Proxy Socket.IO to backend
    location /socket.io/ {
        proxy_pass $backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
    }

    # Vite HMR websocket
    location /__vite_hmr {
        proxy_pass $vite;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
    }

    # Everything else to Vite dev server
    location / {
        proxy_pass $vite;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}